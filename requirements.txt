# Hermes-NapCat Adapter
# Core dependencies for the NapCat OneBot 11 adapter
#
# Install:
#   pip install -r requirements.txt
#
# These should already be installed as part of Hermes Agent's [all] extras.
# Only install separately if the adapter was cloned standalone.

websockets>=12.0
httpx>=0.27.0

# Development / testing / linting dependencies
# pip install -r requirements.txt
# pre-commit install
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4
pre-commit>=3.7
